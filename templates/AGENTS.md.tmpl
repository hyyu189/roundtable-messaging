codex:
  cwd: ${project_dir}
  role: fast implementation, tests, integration, CLI/runtime review
  always_read: [ROUTING.md, README.md]
  startup: 新对话首回合先执行 `~/.roundtable/bin/rt-codex-wake bind ${project_dir}`(roundtable 唤醒自注册;resume 已绑定线程时跳过),再开始任务
